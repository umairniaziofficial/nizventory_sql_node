<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit</title>
    <link rel="stylesheet" href="/css/output.css">
</head>
<body class="bg-myBodyBg text-gray-200">
    <div class="container mx-auto h-screen flex items-center">
        <div class="w-[40%] h-3/4 mx-auto bg-myCardBg p-8 rounded-lg flex justify-center flex-col shadow-2xl">
            <% if (pageTitle.toLowerCase() === "brand") { %>
                <h2 class="text-5xl font-bold mb-12 text-center">Edit Brand</h2>
                <form action="/edit/brand/<%= brand.bid %>" method="POST">
                    <div class="mb-4">
                        <label for="bid" class="block text-sm text-gray-300 mb-2 font-bold">Brand ID</label>
                        <input type="text" id="bid" name="bid" value="<%= brand.bid %>" class="w-full px-3 py-2 bg-gray-700 shadow-2xl rounded-lg" readonly>
                    </div>
                    <div class="mb-4">
                        <label for="bname" class="block text-sm font-bold text-gray-300 mb-2">Brand Name</label>
                        <input type="text" id="bname" name="bname" value="<%= brand.bname %>" class="w-full px-3 py-2 bg-gray-700 rounded-lg" required>
                    </div>
                    <div class="flex justify-center">
                        <button type="submit" class="bg-green-500 text-black font-bold rounded-lg px-4 py-2">Update Brand</button>
                    </div>
                </form>
            <% } else if (pageTitle.toLowerCase() === "category") { %>
                <h2 class="text-5xl font-bold mb-12 text-center">Edit Category</h2>
                <form action="/edit/category/<%= category.cid %>" method="POST">
                    <div class="mb-4">
                        <label for="cid" class="block text-sm text-gray-300 mb-2 font-bold">Category ID</label>
                        <input type="text" id="cid" name="cid" value="<%= category.cid %>" class="w-full px-3 py-2 bg-gray-700 shadow-2xl rounded-lg" readonly>
                    </div>
                    <div class="mb-4">
                        <label for="category_name" class="block text-sm font-bold text-gray-300 mb-2">Category Name</label>
                        <input type="text" id="category_name" name="category_name" value="<%= category.category_name %>" class="w-full px-3 py-2 bg-gray-700 rounded-lg" required>
                    </div>
                    <div class="flex justify-center">
                        <button type="submit" class="bg-green-500 text-black font-bold rounded-lg px-4 py-2">Update Category</button>
                    </div>
                </form>
            <% } else if (pageTitle.toLowerCase() === "store") { %>
                <h2 class="text-5xl font-bold mb-12 text-center">Edit Store</h2>
                <form action="/edit/store/<%= store.sid %>" method="POST">
                    <div class="mb-4">
                        <label for="sid" class="block text-sm text-gray-300 mb-2 font-bold">Store ID</label>
                        <input type="text" id="sid" name="sid" value="<%= store.sid %>" class="w-full px-3 py-2 bg-gray-700 shadow-2xl rounded-lg" readonly>
                    </div>
                    <div class="mb-4">
                        <label for="sname" class="block text-sm font-bold text-gray-300 mb-2">Store Name</label>
                        <input type="text" id="sname" name="sname" value="<%= store.sname %>" class="w-full px-3 py-2 bg-gray-700 rounded-lg" required>
                    </div>
                    <div class="mb-4">
                        <label for="saddress" class="block text-sm font-bold text-gray-300 mb-2">Store Address</label>
                        <input type="text" id="saddress" name="saddress" value="<%= store.address %>" class="w-full px-3 py-2 bg-gray-700 rounded-lg" required>
                    </div>
                    <div class="mb-4">
                        <label for="snumber" class="block text-sm font-bold text-gray-300 mb-2">Store Mobile Number</label>
                        <input type="text" id="snumber" name="snumber" value="<%= store.mobno %>" class="w-full px-3 py-2 bg-gray-700 rounded-lg" required>
                    </div>
                    <div class="flex justify-center">
                        <button type="submit" class="bg-green-500 text-black font-bold rounded-lg px-4 py-2">Update Store</button>
                    </div>
                </form>
            <% } %>
        </div>
    </div>
</body>
</html>
